import MathPaper.Finite.Empty00
set_option maxRecDepth 1000000
set_option maxHeartbeats 0
namespace MathPaper.Finite

theorem data3rlllr_empty : data3rlllr.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3rlll_empty : data3rlll.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 4881578 ⟨22265220096967428800512, 0⟩ data3rllll data3rlllr (by decide +kernel) data3rllll_empty data3rlllr_empty

theorem data3rllrl_empty : data3rllrl.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3rllrr_empty : data3rllrr.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3rllr_empty : data3rllr.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 5320682 ⟨51632436662313034973184, 0⟩ data3rllrl data3rllrr (by decide +kernel) data3rllrl_empty data3rllrr_empty

theorem data3rll_empty : data3rll.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 5136356 ⟨56151888960371875119104, 0⟩ data3rlll data3rllr (by decide +kernel) data3rlll_empty data3rllr_empty

theorem data3rlrll_empty : data3rlrll.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3rlrlr_empty : data3rlrlr.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3rlrl_empty : data3rlrl.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 6049754 ⟨24423489153591446339584, 0⟩ data3rlrll data3rlrlr (by decide +kernel) data3rlrll_empty data3rlrlr_empty

theorem data3rlrrl_empty : data3rlrrl.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3rlrrr_empty : data3rlrrr.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3rlrr_empty : data3rlrr.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 6696954 ⟨11510768301994760208384, 0⟩ data3rlrrl data3rlrrr (by decide +kernel) data3rlrrl_empty data3rlrrr_empty

theorem data3rlr_empty : data3rlr.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 6233244 ⟨8559289250201231949824, 0⟩ data3rlrl data3rlrr (by decide +kernel) data3rlrl_empty data3rlrr_empty

theorem data3rl_empty : data3rl.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 5716216 ⟨62110187296180060291072, 0⟩ data3rll data3rlr (by decide +kernel) data3rll_empty data3rlr_empty

theorem data3rrlll_empty : data3rrlll.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3rrllr_empty : data3rrllr.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3rrll_empty : data3rrll.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 7068498 ⟨7470931349852368404480, 0⟩ data3rrlll data3rrllr (by decide +kernel) data3rrlll_empty data3rrllr_empty

theorem data3rrlrl_empty : data3rrlrl.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3rrlrr_empty : data3rrlrr.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3rrlr_empty : data3rrlr.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 7743754 ⟨53089729444136089550848, 0⟩ data3rrlrl data3rrlrr (by decide +kernel) data3rrlrl_empty data3rrlrr_empty

theorem data3rrl_empty : data3rrl.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 7434220 ⟨56170335704445584670720, 0⟩ data3rrll data3rrlr (by decide +kernel) data3rrll_empty data3rrlr_empty

theorem data3rrrll_empty : data3rrrll.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3rrrlr_empty : data3rrrlr.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3rrrl_empty : data3rrrl.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 8110042 ⟨32355589105286553534464, 0⟩ data3rrrll data3rrrlr (by decide +kernel) data3rrrll_empty data3rrrlr_empty

theorem data3rrrrl_empty : data3rrrrl.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3rrrrr_empty : data3rrrrr.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3rrrr_empty : data3rrrr.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 8638438 ⟨18852572443331161751552, 0⟩ data3rrrrl data3rrrrr (by decide +kernel) data3rrrrl_empty data3rrrrr_empty

theorem data3rrr_empty : data3rrr.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 8372136 ⟨39328458365148764045312, 0⟩ data3rrrl data3rrrr (by decide +kernel) data3rrrl_empty data3rrrr_empty

theorem data3rr_empty : data3rr.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 7987180 ⟨44493546705787438497792, 0⟩ data3rrl data3rrr (by decide +kernel) data3rrl_empty data3rrr_empty

theorem data3r_empty : data3r.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 6822140 ⟨38479908137758124670976, 0⟩ data3rl data3rr (by decide +kernel) data3rl_empty data3rr_empty

theorem data3_empty : data3.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 4644756 ⟨17708874310761169551360, 0⟩ data3l data3r (by decide +kernel) data3l_empty data3r_empty

end MathPaper.Finite
