import MathPaper.Finite.Data3
set_option maxRecDepth 1000000
set_option maxHeartbeats 0
namespace MathPaper.Finite

theorem data3lllll_empty : data3lllll.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3llllr_empty : data3llllr.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3llll_empty : data3llll.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 432510 ⟨37981846047767966777344, 0⟩ data3lllll data3llllr (by decide +kernel) data3lllll_empty data3llllr_empty

theorem data3lllrl_empty : data3lllrl.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3lllrr_empty : data3lllrr.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3lllr_empty : data3lllr.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 863054 ⟨4298091369174325526528, 0⟩ data3lllrl data3lllrr (by decide +kernel) data3lllrl_empty data3lllrr_empty

theorem data3lll_empty : data3lll.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 675732 ⟨33407053517487997976576, 0⟩ data3llll data3lllr (by decide +kernel) data3llll_empty data3lllr_empty

theorem data3llrll_empty : data3llrll.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3llrlr_empty : data3llrlr.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3llrl_empty : data3llrl.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 1415990 ⟨21896285215493237768192, 0⟩ data3llrll data3llrlr (by decide +kernel) data3llrll_empty data3llrlr_empty

theorem data3llrrl_empty : data3llrrl.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3llrrr_empty : data3llrrr.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3llrr_empty : data3llrr.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 2027370 ⟨30676935394578984337408, 0⟩ data3llrrl data3llrrr (by decide +kernel) data3llrrl_empty data3llrrr_empty

theorem data3llr_empty : data3llr.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 1748964 ⟨6105876867933669442840, 3⟩ data3llrl data3llrr (by decide +kernel) data3llrl_empty data3llrr_empty

theorem data3ll_empty : data3ll.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 1167356 ⟨27651669366490617872384, 0⟩ data3lll data3llr (by decide +kernel) data3lll_empty data3llr_empty

theorem data3lrlll_empty : data3lrlll.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3lrllr_empty : data3lrllr.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3lrll_empty : data3lrll.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 2460026 ⟨442721857769029238784, 0⟩ data3lrlll data3lrllr (by decide +kernel) data3lrlll_empty data3lrllr_empty

theorem data3lrlrl_empty : data3lrlrl.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3lrlrr_empty : data3lrlrr.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3lrlr_empty : data3lrlr.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 2890586 ⟨51189719594364621096024, 5⟩ data3lrlrl data3lrlrr (by decide +kernel) data3lrlrl_empty data3lrlrr_empty

theorem data3lrl_empty : data3lrl.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 2703344 ⟨33001225147866387841024, 0⟩ data3lrll data3lrlr (by decide +kernel) data3lrll_empty data3lrlr_empty

theorem data3lrrll_empty : data3lrrll.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3lrrlr_empty : data3lrrlr.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3lrrl_empty : data3lrrl.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 3530714 ⟨34624538626352828383232, 0⟩ data3lrrll data3lrrlr (by decide +kernel) data3lrrll_empty data3lrrlr_empty

theorem data3lrrrl_empty : data3lrrrl.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3lrrrr_empty : data3lrrrr.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

theorem data3lrrr_empty : data3lrrr.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 4205738 ⟨3947603231773844045824, 0⟩ data3lrrrl data3lrrrr (by decide +kernel) data3lrrrl_empty data3lrrrr_empty

theorem data3lrr_empty : data3lrr.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 3959952 ⟨18520531050004389822464, 0⟩ data3lrrl data3lrrr (by decide +kernel) data3lrrl_empty data3lrrr_empty

theorem data3lr_empty : data3lr.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 3229984 ⟨52739241306735608070144, 0⟩ data3lrl data3lrr (by decide +kernel) data3lrl_empty data3lrr_empty

theorem data3l_empty : data3l.all (fun _ d => !ownerRetained d.owner) = true := by
  exact Tree.all_node_true (fun _ d => !ownerRetained d.owner) 2166696 ⟨66463618897575514472448, 0⟩ data3ll data3lr (by decide +kernel) data3ll_empty data3lr_empty

theorem data3rllll_empty : data3rllll.all (fun _ d => !ownerRetained d.owner) = true := by
  decide +kernel

end MathPaper.Finite
